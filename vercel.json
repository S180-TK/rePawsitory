{
  "version": 2,
  "buildCommand": "cd pet-health-frontend && npm install && npm run build",
  "outputDirectory": "pet-health-frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
